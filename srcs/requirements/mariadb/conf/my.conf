[mysqld]

#Docker escucha todas las redes internas
bind-address=0.0.0.0
port=3306

#Formato UNICODE para la transformación de carácteres
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

#Saltamos resolución de DNS, (en docker no son estáticas y cambian)
skip-name-resolve
